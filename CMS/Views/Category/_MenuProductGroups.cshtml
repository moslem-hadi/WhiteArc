@model List<ViewModels.ProductGroupListUiViewModel>
@if (Model.Count > 0)
{
    <ul class="sub-menu">

        @foreach (var item in Model)
        {

            <li><a href="/category/@item.Url">@item.Title</a></li>
        }

    </ul>
}