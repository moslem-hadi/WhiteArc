@model List<ViewModels.ProductGroupListUiViewModel>

<div class="product-box">
    @if (Model.Count > 0)
    {
        <ul class="products row"> 
            @foreach (var item in Model)
            {
                @Html.Partial("_ProductGroupBox", @item)
            }

        </ul>
    }
</div>
